<div class="container">
  <div class="row justify-content-center">
    <div class="col-sm-12 col-md-6 col-lg-3">
      <p class="title">How sustainable is your lifestyle?</h1>
      <p>Fill in the form below to find out your result compared to your local peers...</p>
      <p>How many planets does it take to support your country's lifestyle?</p>
      <!-- To select country, used https://github.com/stefanpenner/country_select -->
      <%= form_for :quiz, url: compute_result_path, class: 'form-quiz' do |f| %>
      <%= f.label :country_code,  'I currently live in...', class: 'label-quiz' %>
      <br>
      <%= f.country_select :country_code%>
      <br>
      <%= f.label :food, 'Compared to your peers, how much animal-based products do you eat? (e.g. beef, pork, chicken, fish, eggs, dairy products)' %>
      <br>
      <%= radio_button("quiz", "food", "-10" ) %>
      <%= radio_button("quiz", "food", "0" ) %>
      <%= radio_button("quiz", "food", "10" ) %>
      <br>
      <%= f.label :home, 'Which is the size of your home (approximatly space/person)? ' %>
      <br>
      <%= radio_button("quiz", "home", "-10" ) %>
      <%= radio_button("quiz", "home", "0" ) %>
      <%= radio_button("quiz", "home", "10" ) %>
      <br>

      <%= f.label :mobility1, 'How many hours do you fly one average each year?' %>
      <br>
      <%= radio_button("quiz", "mobility1", "-10" ) %>
      <%= radio_button("quiz", "mobility1", "0" ) %>
      <%= radio_button("quiz", "mobility1", "10" ) %>
      <br>
      <%= f.label :mobility2, 'What is your usual transport mode?' %>
      <br>
      <%= radio_button("quiz", "mobility2", "-10" ) %>
      <%= radio_button("quiz", "mobility2", "0" ) %>
      <%= radio_button("quiz", "mobility2", "10" ) %>
      <br>
      <%= f.label :product, 'Do you usually try to buy locally sourced goods? \(e.g. clothes, furniture, etc.)' %>
      <br>
      <%= radio_button("quiz", "product", "-10" ) %>
      <%= radio_button("quiz", "product", "0" ) %>
      <%= radio_button("quiz", "product", "10" ) %>
      <br>
      <%= f.submit "Submit", class: "btn custom" %>
      <% end %>
    </div>
  </div>
</div>
<!-- Which country are you currently living in?  -->
